<x-guest-layout>

    <!-- Hero Section -->
    @include('beranda.partials.hero')

    <!-- Events Section -->
    @include('beranda.partials.event')

    <!-- Kretaor Populer -->
    @include('beranda.partials.creator-list')


</x-guest-layout>
